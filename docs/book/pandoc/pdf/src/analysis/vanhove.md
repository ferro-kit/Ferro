[Header 1 ("van-hove-self-correlation-function", [], []) [Str "Van Hove Self-Correlation Function"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The van Hove self-correlation function $G_s(r, \\tau)$ is the probability distribution of single-particle displacements over a time interval $\\tau$.  It provides a more complete picture of atomic motion than the MSD alone: it can reveal non-Gaussian dynamics, heterogeneous mobility, and jump diffusion."], Header 3 ("definition", ["unnumbered", "unlisted"], []) [Str "Definition"], Para [Str "$$G_s(r, \\tau) = \\frac{1}{N} \\sum_j \\langle \\delta!\\left(r - |\\mathbf{r}_j(t_0 + \\tau) - \\mathbf{r}", Emph [Str "j(t_0)|\\right) \\rangle"], Str "{t_0}$$"], Para [Str "In practice this is discretised as a histogram $P(r_i, \\tau)$ normalised so that $\\sum_i P(r_i, \\tau) = 1$ (discrete probability mass function)."], Header 3 ("gaussian-reference", ["unnumbered", "unlisted"], []) [Str "Gaussian Reference"], Para [Str "For a purely diffusive Gaussian process:"], Para [Math DisplayMath "G_s^\\text{Gaussian}(r, \\tau) = \\left(\\frac{1}{4\\pi D\\tau}\\right)^{3/2} \\exp!\\left(-\\frac{r^2}{4D\\tau}\\right) \\cdot 4\\pi r^2"], Para [Str "Deviations from this Gaussian form — e.g. a secondary peak at large $r$ — indicate heterogeneous dynamics or discrete jump events."], Header 3 ("non-gaussian-parameter", ["unnumbered", "unlisted"], []) [Str "Non-Gaussian Parameter"], Para [Str "The non-Gaussian parameter $\\alpha_2(\\tau)$ quantifies the deviation from Gaussian behaviour:"], Para [Math DisplayMath "\\alpha_2(\\tau) = \\frac{3\\langle r^4(\\tau)\\rangle}{5\\langle r^2(\\tau)\\rangle^2} - 1"], Para [Str "$\\alpha_2 = 0$ for a Gaussian distribution; $\\alpha_2 > 0$ indicates fat tails (fast-moving particles)."], Header 3 ("algorithm", ["unnumbered", "unlisted"], []) [Str "Algorithm"], Para [Str "Follows code1/vanhove.c (", Code ("", [], []) "EstimateVanHove", Str "):"], OrderedList (1, DefaultStyle, DefaultDelim) [[Plain [Str "Convert Cartesian positions to fractional coordinates."]], [Plain [Str "Unwrap fractional coordinates (same procedure as MSD)."]], [Plain [Str "Convert back to absolute Cartesian positions."]], [Plain [Str "For each time origin $p$ and each selected atom $j$:
", Math DisplayMath "r = |\\mathbf{r}_j(p + \\tau) - \\mathbf{r}_j(p)|", Str "
Accumulate into histogram bin $\\lfloor r / \\Delta r \\rfloor$."]], [Plain [Str "Normalise: $P(r_i) = \\text{count}(r_i) / (N_\\text{origins} \\cdot N_\\text{atoms})$."]]], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct VanHoveParams {
    pub tau: Option<usize>,            // lag [frames]; None = n_frames - 1
    pub shift: usize,                  // origin spacing [frames]; default: 1
    pub dt: f64,                       // time step [fs]; default: 1.0
    pub r_min: f64,                    // default: 0.0 Å
    pub r_max: f64,                    // default: 10.0 Å
    pub dr: f64,                       // bin width [Å]; default: 0.01
    pub elements: Option<Vec<String>>, // None = all atoms
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "Columns: ", Code ("", [], []) "r[Å]", Str ", ", Code ("", [], []) "Gs(r,tau)", Str " (normalised so sum = 1)."], Para [Str "The header records the lag time $\\tau$ in both frames and physical time (fs)."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "fe-corr -m vanhove -i traj.dump --tau 500 --dt 2.0 -o output.vanhove
", CodeBlock ("", ["rust"], []) "use ferro_analysis::md::{VanHoveParams, calc_vanhove, write_vanhove};

let params = VanHoveParams {
    tau: Some(500), shift: 1, dt: 2.0,
    r_min: 0.0, r_max: 8.0, dr: 0.02,
    elements: Some(vec![\"Li\".into()]),
};
let result = calc_vanhove(&traj, &params).unwrap();
write_vanhove(&result, \"output.vanhove\").unwrap();
", Header 2 ("interpreting-results", ["unnumbered", "unlisted"], []) [Str "Interpreting Results"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault)] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Feature"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Interpretation"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Single peak near $r = 0$"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Localised, non-diffusing atoms"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Broad peak with Gaussian shape"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Normal diffusion"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Bimodal distribution"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Coexistence of slow and fast populations"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Secondary peak at $r \\approx$ jump length"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Discrete jump mechanism"]]]])] (TableFoot ("", [], []) []), Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-origin ", Code ("", [], []) "par_iter", Str "."]], [Plain [Str "For periodic systems, fractional-coordinate unwrapping (shared with MSD) is applied before computing displacements."]], [Plain [Str "For non-periodic systems, raw Cartesian distances are used directly."]]]]