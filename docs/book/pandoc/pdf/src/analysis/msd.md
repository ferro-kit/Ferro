[Header 1 ("mean-squared-displacement-msd", [], []) [Str "Mean Squared Displacement (MSD)"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The mean squared displacement (MSD) quantifies how far atoms diffuse over time.  In the diffusive regime it grows linearly, and the self-diffusion coefficient $D$ is extracted via the Einstein relation."], Header 3 ("einstein-relation", ["unnumbered", "unlisted"], []) [Str "Einstein Relation"], Para [Math DisplayMath "\\text{MSD}(t) = \\langle |\\mathbf{r}(t_0 + t) - \\mathbf{r}(t_0)|^2 \\rangle"], Para [Math DisplayMath "D = \\lim_{t \\to \\infty} \\frac{\\text{MSD}(t)}{6t}"], Para [Str "(factor of 6 for 3-D isotropic diffusion; use 2 for 1-D or 4 for 2-D)."], Header 3 ("time-shift-averaging", ["unnumbered", "unlisted"], []) [Str "Time-Shift Averaging"], Para [Str "To reduce statistical noise, the MSD is averaged over all possible time origins $t_0$ separated by ", Code ("", [], []) "shift", Str " frames:"], Para [Math DisplayMath "\\text{MSD}(\\tau) = \\frac{1}{N_\\text{origins}} \\sum_{p} \\frac{1}{N_\\text{atoms}} \\sum_j \\left|\\mathbf{r}_j(p+\\tau) - \\mathbf{r}_j(p)\\right|^2"], Para [Str "where $p$ runs over ${0, \\text{shift}, 2\\cdot\\text{shift}, \\ldots}$ subject to $p + \\tau \\leq N_\\text{frames}$."], Header 3 ("periodic-boundary-handling", ["unnumbered", "unlisted"], []) [Str "Periodic Boundary Handling"], Para [Str "Atoms crossing periodic boundaries must be ", Strong [Str "unwrapped"], Str " to obtain true displacements."], Para [Strong [Str "Algorithm"], Str " (matching code1/msd.c ", Code ("", [], []) "EstimateMSD", Str "):"], OrderedList (1, DefaultStyle, DefaultDelim) [[Plain [Str "Convert Cartesian coordinates to fractional: $\\mathbf{f}_j^{(t)} = \\mathbf{r}_j^{(t)} \\cdot \\mathbf{M}^{-1}$"]], [Plain [Str "Unwrap: for each step $t > 0$ and each fractional component $k$:
", Math DisplayMath "f_{j,k}^{(t)} \\leftarrow f_{j,k}^{(t)} - \\text{round}!\\left(f_{j,k}^{(t)} - f_{j,k}^{(t-1)}\\right)"]], [Plain [Str "Compute unwrapped Cartesian displacement $\\Delta\\mathbf{r}_j = \\mathbf{\\Delta f}_j \\cdot \\overline{\\mathbf{M}}$"]]], Header 3 ("npt-trajectories", ["unnumbered", "unlisted"], []) [Str "NPT Trajectories"], Para [Str "For variable-box (NPT) simulations, the box matrix changes each frame.  The total MSD uses the ", Strong [Str "average"], Str " of the origin- and endpoint-frame matrices:"], Para [Math DisplayMath "\\overline{\\mathbf{M}} = \\frac{\\mathbf{M}^{(p)} + \\mathbf{M}^{(p+\\tau)}}{2}"], Para [Str "The directional MSD along axis $k$ uses the endpoint cell parameter $L_k^{(p+\\tau)}$ (simplified approximation matching code1):"], Para [Math DisplayMath "\\text{MSD}_k(\\tau) = \\langle (\\Delta f_k)^2 \\rangle \\cdot \\left(L_k^{(p+\\tau)}\\right)^2"], Header 3 ("directional-msd", ["unnumbered", "unlisted"], []) [Str "Directional MSD"], Para [Str "For periodic systems, ", Code ("", [], []) "msd_a", Str ", ", Code ("", [], []) "msd_b", Str ", ", Code ("", [], []) "msd_c", Str " give displacements along the three crystal axes.  For non-periodic systems they correspond to Cartesian $x$, $y$, $z$."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct MsdParams {
    pub tau: Option<usize>,           // window size [frames]; None = all frames
    pub shift: usize,                 // origin spacing [frames]; default: 1
    pub dt: f64,                      // time step [fs]; default: 1.0
    pub elements: Option<Vec<String>>,// None = all atoms
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "Columns: ", Code ("", [], []) "time[fs]", Str ", ", Code ("", [], []) "msd[Å²]", Str ", ", Code ("", [], []) "msd_a[Å²]", Str ", ", Code ("", [], []) "msd_b[Å²]", Str ", ", Code ("", [], []) "msd_c[Å²]"], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "fe-traj -m msd -i traj.dump --dt 2.0 --shift 10 -o output.msd
", CodeBlock ("", ["rust"], []) "use ferro_analysis::md::{MsdParams, calc_msd, write_msd};

let params = MsdParams { tau: Some(1000), shift: 10, dt: 2.0,
    elements: Some(vec![\"Li\".into()]) };
let result = calc_msd(&traj, &params).unwrap();
write_msd(&result, \"output.msd\").unwrap();
", Header 2 ("extracting-the-diffusion-coefficient", ["unnumbered", "unlisted"], []) [Str "Extracting the Diffusion Coefficient"], Para [Str "Fit the linear region (avoiding the ballistic regime at short $t$ and the noise-dominated long-$t$ tail):"], Para [Math DisplayMath "D = \\frac{1}{6} \\cdot \\frac{d,\\text{MSD}}{d,t}"], Para [Str "To convert from Å²/fs to cm²/s: multiply by $10^{-16}$."], Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-origin ", Code ("", [], []) "par_iter", Str "; ", Code ("", [], []) "unwrap_frac", Str " runs serially before parallelisation (sequential dependency)."]], [Plain [Str "Non-periodic path: Cartesian coordinates are used directly without unwrapping."]]]]