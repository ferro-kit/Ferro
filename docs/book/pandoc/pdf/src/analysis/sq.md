[Header 1 ("structure-factor-sq", [], []) [Str "Structure Factor S(q)"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The static structure factor $S(q)$ is computed from the radial distribution function $g(r)$ via a Fourier sine transform (Faber-Ziman formalism)."], Header 3 ("partial-sq", ["unnumbered", "unlisted"], []) [Str "Partial S(q)"], Para [Math DisplayMath "S_{\\alpha\\beta}(q) = 1 + \\frac{4\\pi\\rho}{q} \\int_0^\\infty r\\left[g_{\\alpha\\beta}(r) - 1\\right] \\sin(qr) , dr"], Para [Str "In discretised form:"], Para [Math DisplayMath "S_{\\alpha\\beta}(q) = 1 + \\frac{4\\pi\\rho}{q} \\sum_{r_i} r_i \\left[g_{\\alpha\\beta}(r_i) - 1\\right] \\sin(q r_i) , \\Delta r"], Para [Str "where $\\rho = N/V$ is the total number density."], Header 3 ("weighted-total-sq", ["unnumbered", "unlisted"], []) [Str "Weighted Total S(q)"], Para [Str "For experimental comparison, the total S(q) can be weighted by scattering factors."], Para [Strong [Str "Faber-Ziman weights:"]], Para [Math DisplayMath "S_\\text{weighted}(q) = \\sum_{\\alpha \\leq \\beta} w_{\\alpha\\beta}(q) \\cdot S_{\\alpha\\beta}(q)"], Para [Math DisplayMath "w_{\\alpha\\beta}(q) = \\frac{(2 - \\delta_{\\alpha\\beta}) , c_\\alpha c_\\beta f_\\alpha(q) f_\\beta(q)}{\\left[\\sum_\\gamma c_\\gamma f_\\gamma(q)\\right]^2}"], Para [Str "where $c_\\alpha = N_\\alpha / N$ is the mole fraction of species $\\alpha$, and $f_\\alpha(q)$ is the scattering factor."], BulletList [[Plain [Strong [Str "X-ray"], Str " (", Code ("", [], []) "Xrd", Str "): $f_\\alpha(q)$ are the $q$-dependent atomic form factors."]], [Plain [Strong [Str "Neutron"], Str " (", Code ("", [], []) "Neutron", Str "): $f_\\alpha$ is replaced by the $q$-independent coherent scattering length $b_\\alpha^\\text{coh}$."]]], Header 3 ("physical-interpretation", ["unnumbered", "unlisted"], []) [Str "Physical Interpretation"], Para [Str "$S(q)$ characterises structural correlations at length scale $2\\pi/q$. Key features:"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, (ColWidth 0.5)), (AlignDefault, (ColWidth 0.5))] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Feature"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Interpretation"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "First sharp diffraction peak (FSDP) at $q \\approx 1$–2 Å⁻¹"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Medium-range order (network periodicity ", Str "~", Str "3–5 Å)"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Principal peak at $q \\approx 2$–3 Å⁻¹"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Nearest-neighbour distance"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$S(q) \\to 1$ as $q \\to \\infty$"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Loss of structural correlations at short wavelengths"]]]])] (TableFoot ("", [], []) []), Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct SqParams {
    pub q_min: f64,              // default: 0.1 Å⁻¹
    pub q_max: f64,              // default: 25.0 Å⁻¹
    pub dq: f64,                 // default: 0.05 Å⁻¹
    pub weighting: SqWeighting,  // default: None
}

pub enum SqWeighting {
    None,     // equal weights (Faber-Ziman, no form factors)
    Xrd,      // X-ray atomic form factors
    Neutron,  // neutron coherent scattering lengths
    Both,     // compute both simultaneously
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], CodeBlock ("", ["bash"], []) "nex-traj -m sq -i traj.dump -o output
# writes output.gr, output.sq
", Para [Str "The ", Code ("", [], []) ".sq", Str " file header records both the g(r) parameters (used as input) and the S(q) parameters.", LineBreak, Str "Column ordering matches the ", Code ("", [], []) ".gr", Str " file. Additional columns ", Code ("", [], []) "total_xrd", Str " and/or ", Code ("", [], []) "total_neutron", Str " are appended when weighting is requested."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["rust"], []) "use nexflux_analysis::md::{GrParams, SqParams, SqWeighting, calc_gr, calc_sq_from_gr, write_sq};

let gr = calc_gr(&traj, &GrParams::with_auto_rmax(&traj)).unwrap();
let sq = calc_sq_from_gr(&gr, &SqParams {
    q_min: 0.5, q_max: 20.0, dq: 0.02,
    weighting: SqWeighting::Neutron,
});
write_sq(&gr, &sq, \"output.sq\").unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "The Fourier transform is parallelised over $q$ values with ", Code ("", [], []) "rayon::par_iter", Str "."]], [Plain [Str "Scattering data (form factors and neutron lengths) are stored as static tables in ", Code ("", [], []) "nexflux_analysis::md::scattering_data", Str "."]], [Plain [Str "For a single-element system, ", Code ("", [], []) "total_xrd ≈ total", Str " because all Faber-Ziman weights reduce to 1."]]]]