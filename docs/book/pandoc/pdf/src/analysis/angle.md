[Header 1 ("bond-angle-distribution", [], []) [Str "Bond Angle Distribution"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The bond angle distribution $P(\\theta)$ describes the probability of observing a three-body angle A–B–C, where B is the central atom.  It is a key structural descriptor for network-forming glasses (e.g. P–O–P, O–P–O angles in phosphate glasses)."], Header 3 ("angle-calculation", ["unnumbered", "unlisted"], []) [Str "Angle Calculation"], Para [Str "For a triplet (A, B, C) with B as centre:"], Para [Math DisplayMath "\\theta = \\arccos!\\left(\\frac{\\overrightarrow{BA} \\cdot \\overrightarrow{BC}}{|\\overrightarrow{BA}||\\overrightarrow{BC}|}\\right)"], Para [Str "The vectors $\\overrightarrow{BA}$ and $\\overrightarrow{BC}$ use the minimum-image convention for periodic cells."], Header 3 ("selection-criteria", ["unnumbered", "unlisted"], []) [Str "Selection Criteria"], Para [Str "An atom pair (A, B) forms a bond if $|\\overrightarrow{BA}| < r_\\text{cut,AB}$.", LineBreak, Str "Similarly for (B, C): $|\\overrightarrow{BC}| < r_\\text{cut,BC}$."], Para [Str "When both end atoms are the same element: $r_\\text{cut} = \\min(r_\\text{cut,AB}, r_\\text{cut,BC})$."], Header 3 ("canonical-key-convention", ["unnumbered", "unlisted"], []) [Str "Canonical Key Convention"], Para [Str "For each triplet, the key ", Code ("", [], []) "\"ElemA-ElemCenter-ElemC\"", Str " is assigned with $Z(\\text{ElemA}) \\leq Z(\\text{ElemC})$.  When both end atoms have the same Z, lexicographic order on the label string is used.  This ensures each angle type appears exactly once."], Para [Strong [Str "No double-counting"], Str ": the enumeration requires $\\text{idx}(A) < \\text{idx}(C)$ to avoid counting (A,B,C) and (C,B,A) as separate events."], Header 3 ("statistics", ["unnumbered", "unlisted"], []) [Str "Statistics"], Para [Str "From the raw count histogram $h(\\theta_i)$:"], Para [Math DisplayMath "\\bar{\\theta} = \\frac{\\sum_i \\theta_i , h_i}{\\sum_i h_i}, \\quad \\sigma = \\sqrt{\\frac{\\sum_i (\\theta_i - \\bar{\\theta})^2 h_i}{\\sum_i h_i}}"], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct AngleParams {
    pub r_cut_ab: f64,   // cutoff for lower-Z end to centre [Å]; default: 2.3
    pub r_cut_bc: f64,   // cutoff for higher-Z end to centre [Å]; default: 2.3
    pub d_angle: f64,    // histogram bin width [degrees]; default: 0.1
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "Columns: ", Code ("", [], []) "angle[deg]", Str ", then one column per triplet key in (Z_center, Z_left, Z_right) order."], Para [Str "The header lists mean, standard deviation, and total count per triplet."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "fe-traj -m angle -i traj.dump --rcut-ab 2.3 --rcut-bc 2.3 -o output.angle
", CodeBlock ("", ["rust"], []) "use ferro_analysis::md::{AngleParams, calc_angle, write_angle};

let params = AngleParams { r_cut_ab: 2.4, r_cut_bc: 2.4, d_angle: 0.5 };
let result = calc_angle(&traj, &params).unwrap();
write_angle(&result, \"output.angle\").unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], Header 3 ("linked-cell-list", ["unnumbered", "unlisted"], []) [Str "Linked-Cell List"], Para [Str "Brute-force neighbour search is O(N²) per frame.  ferro uses a linked-cell list to reduce this to O(N·k) where $k$ is the average number of atoms in the search volume."], Para [Str "For each central atom B, only the 27 cells within ±1 cell in each direction are searched. The cell size is chosen so that a sphere of radius $r_\\text{cut,max}$ fits within the search volume:"], Para [Math DisplayMath "n_k = \\left\\lfloor \\frac{1}{r_\\text{cut,max} \\cdot |({\\mathbf{M}^T})^{-1}|_k} \\right\\rfloor"], Para [Str "This is exact for triclinic cells."], Header 3 ("parallelism", ["unnumbered", "unlisted"], []) [Str "Parallelism"], Para [Str "Computation is parallelised per frame with ", Code ("", [], []) "rayon::par_iter().fold().reduce()", Str "."]]