[Header 1 ("radial-distribution-function-gr", [], []) [Str "Radial Distribution Function g(r)"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The radial distribution function (RDF) $g_{\\alpha\\beta}(r)$ describes the probability of finding a particle of type $\\beta$ at distance $r$ from a particle of type $\\alpha$, relative to an ideal gas at the same number density."], Header 3 ("partial-gr", ["unnumbered", "unlisted"], []) [Str "Partial g(r)"], Para [Str "For a system with $N_\\alpha$ atoms of type $\\alpha$ and $N_\\beta$ atoms of type $\\beta$ in a volume $V$:"], Para [Math DisplayMath "g_{\\alpha\\beta}(r) = \\frac{V}{N_\\alpha N_\\beta} \\frac{\\langle n_{\\alpha\\beta}(r, r+\\Delta r)\\rangle}{4\\pi r^2 \\Delta r}"], Para [Str "where $n_{\\alpha\\beta}(r, r+\\Delta r)$ is the number of $\\beta$ atoms in the shell $[r, r+\\Delta r)$ around any $\\alpha$ atom."], Para [Str "In practice, the histogram count $C_{\\alpha\\beta}$ is accumulated and normalised as follows:"], Para [Strong [Str "Same species"], Str " ($\\alpha = \\beta$):
", Math DisplayMath "g_{\\alpha\\alpha}(r_i) = \\frac{2 , C_{\\alpha\\alpha}(r_i)}{4\\pi r_i^2 \\Delta r \\cdot \\frac{N_\\alpha - 1}{V} \\cdot N_\\alpha \\cdot N_\\text{frames}}"], Para [Strong [Str "Different species"], Str " ($\\alpha \\neq \\beta$):
", Math DisplayMath "g_{\\alpha\\beta}(r_i) = \\frac{C_{\\alpha\\beta}(r_i)}{4\\pi r_i^2 \\Delta r \\cdot \\frac{N_\\beta}{V} \\cdot N_\\alpha \\cdot N_\\text{frames}}"], Para [Str "The bin centre is $r_i = r_\\text{min} + (i + 0.5) \\Delta r$."], Header 3 ("total-gr", ["unnumbered", "unlisted"], []) [Str "Total g(r)"], Para [Str "All atoms are treated as a single species:"], Para [Math DisplayMath "g_\\text{total}(r_i) = \\frac{2 , C_\\text{total}(r_i)}{4\\pi r_i^2 \\Delta r \\cdot \\frac{N-1}{V} \\cdot N \\cdot N_\\text{frames}}"], Header 3 ("coordination-number-cnr", ["unnumbered", "unlisted"], []) [Str "Coordination Number CN(r)"], Para [Str "The cumulative coordination number gives the average number of neighbours within distance $r$."], Para [Strong [Str "Directed CN"], Str " (", Code ("", [], []) "\"A-B\"", Str " = average B atoms within $r$ around each A):"], Para [Str "$$\\text{CN}", Emph [Str "{A \\to B}(r) = \\sum"], Str "{r_i \\leq r} \\frac{m \\cdot C_{AB}(r_i)}{N_A \\cdot N_\\text{frames}}$$"], Para [Str "where $m = 2$ for same-species pairs and $m = 1$ for cross-species pairs.", LineBreak, Str "For $A \\neq B$: the reverse direction ", Code ("", [], []) "\"B-A\"", Str " is also computed with $N_B$ as the denominator."], Header 3 ("minimum-image-convention", ["unnumbered", "unlisted"], []) [Str "Minimum-Image Convention"], Para [Str "All pair distances use the minimum-image convention:"], Para [Str "$$\\mathbf{r}", Emph [Str "{ij}^\\text{min} = \\mathbf{r}"], Str "{ij} - \\mathbf{M} \\cdot \\text{round}!\\left(\\mathbf{M}^{-1} \\mathbf{r}_{ij}\\right)$$"], Para [Str "This is correct for orthorhombic and triclinic cells as long as $r_\\text{max} < L_\\text{min}/2$."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct GrParams {
    pub r_min: f64,   // default: 0.005 Å
    pub r_max: f64,   // default: 10.005 Å  (use with_auto_rmax for safety)
    pub dr: f64,      // default: 0.01 Å
    pub r_cut: f64,   // default: 2.3 Å  (for short-range bond statistics)
}
", Para [Code ("", [], []) "r_max", Str " must satisfy $r_\\text{max} < L_\\text{min}/2$.  Use ", Code ("", [], []) "GrParams::with_auto_rmax(&traj)", Str " to set it automatically from the first frame."], Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, (ColWidth 0.5)), (AlignDefault, (ColWidth 0.5))] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "File"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Content"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) ".gr"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Columns: ", Code ("", [], []) "r[Å]", Str ", then symmetric partial g(r) in periodic-table order, ", Code ("", [], []) "total", Str " last"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) ".cn"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Columns: ", Code ("", [], []) "r[Å]", Str ", then directed CN pairs in (Z_center, Z_neighbor) order"]]]])] (TableFoot ("", [], []) []), Para [Str "Header lines record all parameters, atom counts, average volume, and number density."], Header 3 ("bond-statistics", ["unnumbered", "unlisted"], []) [Str "Bond Statistics"], Para [Str "Within ", Code ("", [], []) "r_cut", Str ", the mean and standard deviation of pair distances are computed and printed in the ", Code ("", [], []) ".cn", Str " header:"], Para [Math DisplayMath "\\bar{d} = \\frac{1}{N_\\text{bonds}} \\sum_k d_k, \\quad \\sigma = \\sqrt{\\frac{1}{N_\\text{bonds}} \\sum_k (d_k - \\bar{d})^2}"], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "nex-traj -m gr -i traj.dump -o output
# writes output.gr, output.cn
", CodeBlock ("", ["rust"], []) "use nexflux_analysis::md::{GrParams, calc_gr, write_gr, write_cn};

let params = GrParams::with_auto_rmax(&traj);
let result = calc_gr(&traj, &params).unwrap();
write_gr(&result, \"output.gr\").unwrap();
write_cn(&result, \"output.cn\").unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-frame ", Code ("", [], []) "par_iter", Str " with ", Code ("", [], []) "fold", Str "/", Code ("", [], []) "reduce", Str " accumulation."]], [Plain [Str "Pseudo-element labels (e.g. ", Code ("", [], []) "\"P0\"", Str ", ", Code ("", [], []) "\"Ob\"", Str ") are supported: ", Code ("", [], []) "elem_z", Str " resolves them by stripping numeric/alphabetic suffixes."]], [Plain [Str "Column ordering uses atomic number Z as the primary sort key, with string labels as tiebreaker for deterministic ordering of pseudo-elements."]]]]