[Header 1 ("velocity-autocorrelation-function-vacf", [], []) [Str "Velocity Autocorrelation Function (VACF)"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The velocity autocorrelation function (VACF) $C_v(t)$ measures how quickly atomic velocities decorrelate over time.  Its Fourier transform gives the vibrational density of states (VDOS), which encodes information about lattice dynamics and diffusion mechanisms."], Header 3 ("definition", ["unnumbered", "unlisted"], []) [Str "Definition"], Para [Str "$$C_v(t) = \\langle \\mathbf{v}(t_0) \\cdot \\mathbf{v}(t_0 + t) \\rangle = \\frac{1}{N} \\sum_j \\langle \\mathbf{v}_j(t_0) \\cdot \\mathbf{v}", Emph [Str "j(t_0 + t) \\rangle"], Str "{t_0}$$"], Para [Str "Expanding into Cartesian components:"], Para [Math DisplayMath "C_v(t) = \\frac{1}{N} \\sum_j \\left[ v_{x,j}(0) v_{x,j}(t) + v_{y,j}(0) v_{y,j}(t) + v_{z,j}(0) v_{z,j}(t) \\right]"], Para [Str "At $t = 0$: $C_v(0) = \\langle v^2 \\rangle = 3 k_B T / m$ (equipartition theorem)."], Header 3 ("self-diffusion-coefficient", ["unnumbered", "unlisted"], []) [Str "Self-Diffusion Coefficient"], Para [Str "The Green-Kubo relation connects the VACF to the self-diffusion coefficient $D$:"], Para [Math DisplayMath "D = \\frac{1}{3} \\int_0^\\infty C_v(t) , dt"], Para [Str "In practice, the running integral is computed as:"], Para [Math DisplayMath "D(t) = \\frac{1}{3} \\sum_{i=0}^{n} C_v(i \\cdot \\Delta t) \\cdot \\Delta t"], Para [Str "which converges to $D$ as $t \\to \\infty$ (rectangular approximation, same as code1)."], Header 3 ("vibrational-density-of-states", ["unnumbered", "unlisted"], []) [Str "Vibrational Density of States"], Para [Str "The VDOS $g(\\nu)$ is obtained via Fourier transform of the VACF:"], Para [Math DisplayMath "g(\\nu) \\propto \\int_0^\\infty C_v(t) \\cos(2\\pi \\nu t) , dt"], Para [Str "Peaks in $g(\\nu)$ correspond to characteristic vibrational modes."], Header 3 ("time-shift-averaging", ["unnumbered", "unlisted"], []) [Str "Time-Shift Averaging"], Para [Math DisplayMath "C_v(\\tau) = \\frac{1}{N_\\text{origins}} \\sum_p \\frac{1}{N_\\text{atoms}} \\sum_j \\mathbf{v}_j(p) \\cdot \\mathbf{v}_j(p + \\tau)"], Para [Str "Unlike position-based analyses, ", Strong [Str "no coordinate unwrapping is needed"], Str " — velocities do not have periodic boundary jumps."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct VacfParams {
    pub tau: Option<usize>,            // window [frames]; None = all frames
    pub shift: usize,                  // origin spacing [frames]; default: 1
    pub dt: f64,                       // time step [fs]; default: 1.0
    pub elements: Option<Vec<String>>, // None = all atoms
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "Columns: ", Code ("", [], []) "time[fs]", Str ", ", Code ("", [], []) "vacf[v²]", Str ", ", Code ("", [], []) "vacf_x[v²]", Str ", ", Code ("", [], []) "vacf_y[v²]", Str ", ", Code ("", [], []) "vacf_z[v²]", Str ", ", Code ("", [], []) "diffusion[v²·fs]"], Header 3 ("unit-note", ["unnumbered", "unlisted"], []) [Str "Unit Note"], Para [Str "Velocities are stored in whatever unit the source trajectory uses.  LAMMPS metal-unit dump files store velocities in Å/ps.  To convert to internal standard units (Å/fs), multiply by $10^{-3}$.  As a consequence:"], BulletList [[Plain [Str "$C_v$ in metal units: (Å/ps)² → multiply by $10^{-6}$ to get (Å/fs)²"]], [Plain [Str "$D$ in metal units: Å²/ps → multiply by $10^{-3}$ to get Å²/fs → multiply by $10^{-16}$ to get cm²/s"]]], Para [Str "This conversion will be applied automatically in a future IO unit normalisation update."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "nex-corr -m vacf -i traj.dump --dt 2.0 --elements Li -o output.vacf
", CodeBlock ("", ["rust"], []) "use nexflux_analysis::md::{VacfParams, calc_vacf, write_vacf};

let params = VacfParams { tau: Some(500), dt: 2.0, shift: 1,
    elements: Some(vec![\"Li\".into()]) };
let result = calc_vacf(&traj, &params).unwrap();
write_vacf(&result, \"output.vacf\").unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Requires ", Code ("", [], []) "frame.velocities", Str " to be populated (returns ", Code ("", [], []) "None", Str " if any frame lacks velocities)."]], [Plain [Str "Parallelism: per-origin ", Code ("", [], []) "par_iter", Str "."]]]]