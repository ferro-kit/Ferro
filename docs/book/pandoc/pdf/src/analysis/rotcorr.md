[Header 1 ("rotational-autocorrelation-function-c₂t", [], []) [Str "Rotational Autocorrelation Function C₂(t)"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The second-order rotational autocorrelation function $C_2(t)$ measures how quickly molecular orientations decorrelate over time.  It is defined using the second-order Legendre polynomial $P_2$:"], Para [Math DisplayMath "C_2(t) = \\langle P_2(\\cos\\theta(t)) \\rangle = \\left\\langle \\frac{3\\cos^2\\theta(t) - 1}{2} \\right\\rangle"], Para [Str "where $\\theta(t)$ is the angle between the orientation vector at time $t_0$ and time $t_0 + t$."], Header 3 ("orientation-vector", ["unnumbered", "unlisted"], []) [Str "Orientation Vector"], Para [Str "For each centre atom $c$, the orientation vector $\\mathbf{u}", Emph [Str "c(t)$ is the vectorial sum of all bond vectors to neighbour atoms within the cutoff radius $r"], Str "\\text{cut}$:"], Para [Str "$$\\mathbf{u}", Emph [Str "c(t) = \\sum"], Str "{n \\in \\text{neighbours}(c,, r_\\text{cut})} (\\mathbf{r}_n - \\mathbf{r}", Emph [Str "c)"], Str "\\text{min-image}$$"], Para [Str "This generalises to arbitrary A-centre-B structures (e.g. water: O as centre, H as neighbour; phosphate tetrahedra: P as centre, O as neighbour)."], Header 3 ("p₂-calculation", ["unnumbered", "unlisted"], []) [Str "P₂ Calculation"], Para [Str "For each centre atom $j$ and time origin $p$:"], Para [Math DisplayMath "P_2!\\left[\\cos\\theta_j(p, \\tau)\\right] = \\frac{3(\\mathbf{u}_j(p) \\cdot \\mathbf{u}_j(p+\\tau))^2}{2|\\mathbf{u}_j(p)|^2 |\\mathbf{u}_j(p+\\tau)|^2} - \\frac{1}{2}"], Para [Str "Atoms with zero orientation vector (no neighbours) are excluded."], Header 3 ("time-shift-averaging", ["unnumbered", "unlisted"], []) [Str "Time-Shift Averaging"], Para [Math DisplayMath "C_2(\\tau) = \\frac{1}{N_\\text{origins}} \\sum_p \\frac{1}{N_\\text{mol}} \\sum_j P_2!\\left[\\cos\\theta_j(p, \\tau)\\right]"], Header 3 ("physical-properties", ["unnumbered", "unlisted"], []) [Str "Physical Properties"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault)] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Property"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Value"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$C_2(0)$"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Always = 1 (angle with itself is 0°)"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$C_2(\\infty)$"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "→ 0 for freely rotating molecules"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Range"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$[-0.5,\\ 1]$"]]]])] (TableFoot ("", [], []) []), Para [Str "$C_2(t)$ for a perfect rotor decays exponentially: $C_2(t) = e^{-t/\\tau_c}$, where $\\tau_c$ is the rotational correlation time."], Header 3 ("rotational-correlation-time", ["unnumbered", "unlisted"], []) [Str "Rotational Correlation Time"], Para [Str "The rotational correlation time $\\tau_c$ is obtained from the running integral:"], Para [Math DisplayMath "\\tau_c(t) = \\int_0^t C_2(t') , dt'"], Para [Str "which converges to $\\tau_c$ as $t \\to \\infty$."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct RotCorrParams {
    pub center: String,      // centre element, e.g. \"O\"
    pub neighbor: String,    // neighbour element, e.g. \"H\"
    pub r_cut: f64,          // cutoff radius [Å]; default: 1.2
    pub tau: Option<usize>,  // window [frames]; None = all frames
    pub shift: usize,        // origin spacing; default: 1
    pub dt: f64,             // time step [fs]; default: 1.0
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "Columns: ", Code ("", [], []) "time[fs]", Str ", ", Code ("", [], []) "C(t)", Str ", ", Code ("", [], []) "integral[fs]"], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "nex-corr -m rotcorr -i traj.dump --center P --neighbor O --rcut 2.4 --dt 2.0 -o output.rotcorr
", CodeBlock ("", ["rust"], []) "use nexflux_analysis::md::{RotCorrParams, calc_rotcorr, write_rotcorr};

let params = RotCorrParams {
    center: \"P\".into(), neighbor: \"O\".into(),
    r_cut: 2.4, tau: Some(500), shift: 5, dt: 2.0,
};
let result = calc_rotcorr(&traj, &params).unwrap();
write_rotcorr(&result, \"output.rotcorr\").unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Orientation vectors are precomputed for all frames before the parallel loop."]], [Plain [Str "For periodic systems, the minimum-image convention is applied to bond vectors."]], [Plain [Str "Parallelism: per-origin ", Code ("", [], []) "par_iter", Str "."]]]]