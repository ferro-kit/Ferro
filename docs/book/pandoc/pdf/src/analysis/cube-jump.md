[Header 1 ("jump-distance-distribution", [], []) [Str "Jump Distance Distribution"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The jump distribution map identifies spatial hotspots of large atomic displacements (jump events).  For each atom and each time window $[t,, t+\\tau]$, the true Cartesian displacement is computed from unwrapped fractional coordinates.  If the displacement exceeds a threshold $d_\\text{min}$, the event is recorded at a voxel position on the 3-D grid."], Para [Str "This analysis is particularly useful for characterising ion hopping in glassy or disordered materials: the resulting cube shows ", Emph [Str "where"], Str " jumps originate, terminate, or pass through, enabling the identification of preferred migration corridors."], Header 3 ("fractional-coordinate-unwrapping", ["unnumbered", "unlisted"], []) [Str "Fractional-Coordinate Unwrapping"], Para [Str "To obtain the minimum-image displacement correctly for periodic (and NPT) trajectories, fractional coordinates are unwrapped in-place before computing differences.  For a single atom over all frames:"], Para [Math DisplayMath "f_{j,k}^{(t)} \\leftarrow f_{j,k}^{(t)} - \\text{round}!\\left(f_{j,k}^{(t)} - f_{j,k}^{(t-1)}\\right), \\quad k \\in {a, b, c}"], Para [Str "This step-by-step correction removes periodic-boundary crossings from the fractional trajectory."], Header 3 ("displacement-calculation", ["unnumbered", "unlisted"], []) [Str "Displacement Calculation"], Para [Str "For each time window $[t,, t+\\tau]$, the unwrapped fractional displacement is:"], Para [Math DisplayMath "\\Delta\\mathbf{f} = \\mathbf{f}^{(t+\\tau)} - \\mathbf{f}^{(t)}"], Para [Str "The true Cartesian displacement uses the average cell matrix of the two endpoint frames (consistent with the MSD NPT treatment):"], Para [Math DisplayMath "\\Delta\\mathbf{r} = \\overline{\\mathbf{M}}^T, \\Delta\\mathbf{f}, \\quad \\overline{\\mathbf{M}} = \\frac{\\mathbf{M}^{(t)} + \\mathbf{M}^{(t+\\tau)}}{2}"], Para [Str "A jump event is recorded when $|\\Delta\\mathbf{r}| > d_\\text{min}$."], Header 3 ("recording-position", ["unnumbered", "unlisted"], []) [Str "Recording Position"], Para [Str "Each jump event is recorded at the atom's wrapped fractional position.  Three choices are available:"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, (ColWidth 0.3333333333333333)), (AlignDefault, (ColWidth 0.3333333333333333)), (AlignDefault, (ColWidth 0.3333333333333333))] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Option"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Recorded position"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Typical use"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "Start"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$\\mathbf{f}^{(t)}$ wrapped"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Identify jump origins"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "End"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$\\mathbf{f}^{(t+\\tau)}$ wrapped"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Identify jump destinations"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "Midpoint"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "$(\\mathbf{f}^{(t)} + \\mathbf{f}^{(t+\\tau)})/2$ wrapped"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Highlight migration pathways"]]]])] (TableFoot ("", [], []) []), Para [Str "The voxel index is computed by folding the fractional coordinate into $[0, 1)$ and multiplying by the grid size."], Header 3 ("grid-value", ["unnumbered", "unlisted"], []) [Str "Grid Value"], Para [Str "Each voxel stores the raw count of jump events recorded at that location.  No normalisation is applied; to convert to a probability density, divide by the total event count."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct CubeJumpParams {
    pub nx: usize,                        // grid divisions along a axis; default: 50
    pub ny: usize,                        // grid divisions along b axis; default: 50
    pub nz: usize,                        // grid divisions along c axis; default: 50
    pub tau: usize,                       // displacement lag [frames]; default: 1
    pub threshold: f64,                   // minimum displacement [Å]; default: 1.0
    pub elements: Option<Vec<String>>,    // None = all atoms
    pub record_at: JumpPosition,          // Start | End | Midpoint; default: Start
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "A Gaussian cube file with raw jump-event counts per voxel.  Typical post-processing:"], OrderedList (1, DefaultStyle, DefaultDelim) [[Plain [Str "Load in VESTA with isosurface rendering to visualise migration channels."]], [Plain [Str "Normalise by total jump count to obtain the spatial probability distribution."]]], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "# Jump origins for Li, lag = 1 frame, threshold = 1.0 Å
nex-cube -m jump -i traj.dump --elements Li --tau 1 --threshold 1.0 -o jump.cube

# Jump midpoints (migration pathway visualisation)
nex-cube -m jump -i traj.dump --elements Li --record-at midpoint -o jump_mid.cube
", CodeBlock ("", ["rust"], []) "use nexflux_analysis::md::{CubeJumpParams, JumpPosition, calc_cube_jump};
use nexflux_io::write_cube;

let params = CubeJumpParams {
    nx: 50, ny: 50, nz: 50,
    tau: 1,
    threshold: 1.0,
    elements: Some(vec![\"Li\".into()]),
    record_at: JumpPosition::Start,
};
let result = calc_cube_jump(&traj, &params).unwrap();
write_cube(\"jump.cube\", &result.cube).unwrap();
println!(\"{} jump events recorded\", result.n_jumps);
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-atom ", Code ("", [], []) "par_iter", Str ".  Each atom independently unwraps its fractional trajectory and accumulates jump events into a local grid; results are reduced by element-wise addition."]], [Plain [Str "Fractional coordinates for all frames are pre-extracted into a ", Code ("", [], []) "Vec<Vec<[f64;3]>>", Str " before parallelisation (shared read-only, zero copying per thread)."]], [Plain [Str "For NPT trajectories, cell matrices are also cached per frame so that the average $\\overline{\\mathbf{M}}$ can be computed without locking."]], [Plain [Str "Atoms absent from some frames (length-mismatch guard) are silently skipped."]]]]