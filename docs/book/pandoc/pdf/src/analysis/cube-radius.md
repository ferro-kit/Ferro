[Header 1 ("hard-sphere-occupancy", [], []) [Str "Hard-Sphere Occupancy"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The hard-sphere occupancy map treats each atom as a sphere of radius $r$.  For each (frame, atom) pair, every voxel whose centre lies within $r$ of the atom is incremented by 1.  The resulting 3-D grid represents the cumulative time-averaged occupancy: high values indicate regions of space that are frequently occupied by the selected atom type."], Para [Str "Unlike the density mode (which bins atoms by position), this method smears each atom over a finite volume, producing smoother maps suitable for visualising preferred coordination sites or channel geometry in disordered materials."], Header 3 ("voxel-inclusion-criterion", ["unnumbered", "unlisted"], []) [Str "Voxel Inclusion Criterion"], Para [Str "For an atom at fractional coordinate $\\mathbf{f}_\\text{atom}$ and a voxel with centre at fractional coordinate $\\mathbf{v}$:"], OrderedList (1, DefaultStyle, DefaultDelim) [[Para [Str "Compute the fractional displacement with minimum-image convention:", SoftBreak, Math DisplayMath "\\Delta\\mathbf{f} = \\mathbf{f}_\\text{atom} - \\mathbf{v}, \\quad \\Delta f_k \\leftarrow \\Delta f_k - \\text{round}(\\Delta f_k)"]], [Para [Str "Convert to Cartesian using the cell matrix transpose:", SoftBreak, Math DisplayMath "\\Delta\\mathbf{r} = \\mathbf{M}^T \\Delta\\mathbf{f}"]], [Para [Str "Increment the voxel count if $|\\Delta\\mathbf{r}| < r$."]]], Para [Str "The voxel centre fractional coordinates are:"], Para [Math DisplayMath "v_k = \\frac{i_k + 0.5}{n_k}, \\quad i_k \\in {0, \\ldots, n_k - 1}"], Header 3 ("bounding-box-optimisation", ["unnumbered", "unlisted"], []) [Str "Bounding-Box Optimisation"], Para [Str "Brute-force comparison of every atom against every voxel is $O(N \\cdot n_x n_y n_z)$.  Instead, for each atom, only the voxels within a bounding box of half-width"], Para [Math DisplayMath "s_k = \\left\\lceil r \\cdot \\left|(\\mathbf{M}^T)^{-1}_k\\right| \\cdot n_k \\right\\rceil + 1"], Para [Str "around the atom's home voxel are checked.  The row norm of $(\\mathbf{M}^T)^{-1}$ gives the maximum fractional offset in direction $k$ corresponding to a Cartesian distance of $r$, making the bound exact for triclinic cells."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct CubeRadiusParams {
    pub nx: usize,                        // grid divisions along a axis; default: 100
    pub ny: usize,                        // grid divisions along b axis; default: 100
    pub nz: usize,                        // grid divisions along c axis; default: 100
    pub radius: f64,                      // hard-sphere radius [Å]; default: 0.7
    pub elements: Option<Vec<String>>,    // None = all atoms
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "A Gaussian cube file with raw occupancy counts per voxel.  Values are not normalised.  To convert to fractional occupancy, divide by $N_\\text{frames} \\cdot N_\\text{atoms}$."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "# Li occupancy map, 100³ grid, radius 0.7 Å
fe-cube -m radius -i traj.dump --elements Li --radius 0.7 --nx 100 --ny 100 --nz 100 -o li_radius.cube

# All-atom occupancy
fe-cube -m radius -i traj.dump --radius 1.0 -o all_radius.cube
", CodeBlock ("", ["rust"], []) "use ferro_analysis::md::{CubeRadiusParams, calc_cube_radius};
use ferro_io::write_cube;

let params = CubeRadiusParams {
    nx: 100, ny: 100, nz: 100,
    radius: 0.7,
    elements: Some(vec![\"Li\".into()]),
};
let result = calc_cube_radius(&traj, &params).unwrap();
write_cube(\"li_radius.cube\", &result.cube).unwrap();
", Header 2 ("comparison-with-density-mode", ["unnumbered", "unlisted"], []) [Str "Comparison with Density Mode"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault)] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Feature"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Density mode"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Radius mode"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Resolution"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Depends on bin width $1/n_k$"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Depends on $r$ and $n_k$"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Smoothing"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "None (point-like atoms)"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Hard-sphere smearing"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Normalisation"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "atoms/Å³"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Raw counts"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Typical use"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Structural analysis"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Site visualisation"]]]])] (TableFoot ("", [], []) []), Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-frame ", Code ("", [], []) "par_iter", Str ".  Each frame independently produces a count array; results are reduced by element-wise addition."]], [Plain [Str "The bounding-box optimisation reduces the inner loop from $O(n_x n_y n_z)$ to $O(s_x s_y s_z)$ per atom, giving roughly $10\\times$ speedup at default settings."]], [Plain [Str "Fractional coordinates are folded into $[0, 1)$ with ", Code ("", [], []) "rem_euclid", Str " before the bounding-box centre is computed, ensuring correct periodic wrapping."]]]]