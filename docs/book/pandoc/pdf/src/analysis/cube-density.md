[Header 1 ("3-d-spatial-density-maps", [], []) [Str "3-D Spatial Density Maps"], Header 2 ("theory", ["unnumbered", "unlisted"], []) [Str "Theory"], Para [Str "The spatial density map divides the simulation box into an $n_x \\times n_y \\times n_z$ voxel grid and accumulates a time-averaged scalar quantity for each voxel.  Three modes are supported:"], Table ("", [], []) (Caption Nothing []) [(AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault), (AlignDefault, ColWidthDefault)] (TableHead ("", [], []) [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Mode"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Accumulated quantity"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "Unit"]]]]) [(TableBody ("", [], []) (RowHeadColumns 0) [] [Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "Density"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "atomic number density"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "atoms/Å³"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "Velocity"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "mean atomic speed $"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "\\mathbf{v}"]]], Row ("", [], []) [Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Code ("", [], []) "Force"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "mean atomic force magnitude $"]], Cell ("", [], []) AlignDefault (RowSpan 0) (ColSpan 0) [Plain [Str "\\mathbf{f}"]]]])] (TableFoot ("", [], []) []), Para [Str "The result is a Gaussian cube file that can be visualised directly in VESTA, VMD, or similar tools."], Header 3 ("density-mode", ["unnumbered", "unlisted"], []) [Str "Density Mode"], Para [Str "Each atom is mapped to a voxel by converting its Cartesian position to fractional coordinates:"], Para [Str "$$\\mathbf{f}", Emph [Str "j = \\mathbf{M}^{-1} \\mathbf{r}", Emph [Str "j, \\quad f"], Str "{j,k} = f"], Str "{j,k} \\bmod 1$$"], Para [Str "The voxel index along axis $k$ is:"], Para [Math DisplayMath "i_k = \\lfloor f_{j,k} \\cdot n_k \\rfloor \\bmod n_k"], Para [Str "After accumulating counts over all atoms and frames, the number density in voxel $(i, j, k)$ is:"], Para [Str "$$\\rho_{ijk} = \\frac{\\text{count}", Emph [Str "{ijk}}{N"], Str "\\text{frames} \\cdot V_\\text{voxel}}$$"], Para [Str "where the voxel volume is:"], Para [Math DisplayMath "V_\\text{voxel} = \\frac{V_\\text{cell}}{n_x \\cdot n_y \\cdot n_z}"], Header 3 ("velocity--force-mode", ["unnumbered", "unlisted"], []) [Str "Velocity / Force Mode"], Para [Str "For ", Code ("", [], []) "Velocity", Str " and ", Code ("", [], []) "Force", Str " modes the average magnitude is computed per voxel:"], Para [Math DisplayMath "\\langle |\\mathbf{q}| \\rangle_{ijk} = \\frac{\\sum_{\\text{visits}} |\\mathbf{q}|}{\\text{count}_{ijk}}"], Para [Str "where $\\mathbf{q}$ is the velocity or force vector of the visiting atom.  Voxels with no visits are set to zero."], Header 3 ("voxel-spacing-matrix", ["unnumbered", "unlisted"], []) [Str "Voxel Spacing Matrix"], Para [Str "The output cube file encodes the voxel step vectors.  For a general triclinic cell with matrix $\\mathbf{M}$ (rows = $\\mathbf{a}, \\mathbf{b}, \\mathbf{c}$), the spacing matrix is:"], Para [Math DisplayMath "\\mathbf{S} = \\begin{pmatrix} \\mathbf{a}/n_x \\ \\mathbf{b}/n_y \\ \\mathbf{c}/n_z \\end{pmatrix}"], Para [Str "This ensures correct spatial mapping for both orthogonal and triclinic simulation cells."], Header 3 ("time-averaged-structure", ["unnumbered", "unlisted"], []) [Str "Time-Averaged Structure"], Para [Str "The cube file header includes a time-averaged atomic structure (mean position over all frames), which serves as a reference geometry for visualisation."], Header 2 ("parameters", ["unnumbered", "unlisted"], []) [Str "Parameters"], CodeBlock ("", ["rust"], []) "pub struct CubeDensityParams {
    pub nx: usize,                        // grid divisions along a axis; default: 50
    pub ny: usize,                        // grid divisions along b axis; default: 50
    pub nz: usize,                        // grid divisions along c axis; default: 50
    pub elements: Option<Vec<String>>,    // None = all atoms
    pub mode: CubeMode,                   // Density | Velocity | Force; default: Density
}
", Header 2 ("output", ["unnumbered", "unlisted"], []) [Str "Output"], Para [Str "A Gaussian cube file (", Code ("", [], []) ".cube", Str "):"], BulletList [[Plain [Str "Header: time-averaged atomic positions + unit cell"]], [Plain [Str "Data: $n_x \\times n_y \\times n_z$ scalar field"]]], Para [Str "The cube format is directly accepted by VESTA, VMD, Ovito, and most electronic-structure visualisation packages."], Header 2 ("usage", ["unnumbered", "unlisted"], []) [Str "Usage"], CodeBlock ("", ["bash"], []) "# Number density of all atoms
fe-cube -m density -i traj.dump --nx 80 --ny 80 --nz 80 -o density.cube

# Li-only density
fe-cube -m density -i traj.dump --elements Li -o li_density.cube

# Time-averaged velocity magnitude
fe-cube -m velocity -i traj.dump --metal-units -o velocity.cube

# Time-averaged force magnitude
fe-cube -m force -i traj.dump -o force.cube
", CodeBlock ("", ["rust"], []) "use ferro_analysis::md::{CubeDensityParams, CubeMode, calc_cube_density};
use ferro_io::write_cube;

let params = CubeDensityParams {
    nx: 80, ny: 80, nz: 80,
    elements: Some(vec![\"Li\".into()]),
    mode: CubeMode::Density,
};
let result = calc_cube_density(&traj, &params).unwrap();
write_cube(\"li_density.cube\", &result.cube).unwrap();
", Header 2 ("implementation-notes", ["unnumbered", "unlisted"], []) [Str "Implementation Notes"], BulletList [[Plain [Str "Parallelism: per-frame ", Code ("", [], []) "par_iter", Str ".  Each frame independently produces ", Code ("", [], []) "(count, value_sum)", Str " arrays; results are reduced by element-wise addition."]], [Plain [Str "Fractional coordinates are folded with ", Code ("", [], []) "rem_euclid(1.0)", Str " to handle atoms outside the nominal box (e.g. from NPT fluctuations or wrapped dump files)."]], [Plain [Code ("", [], []) "Velocity", Str " mode requires ", Code ("", [], []) "frame.velocities", Str " to be populated; ", Code ("", [], []) "Force", Str " mode requires ", Code ("", [], []) "frame.forces", Str ".  Frames missing the required data are silently skipped."]], [Plain [Str "For NPT trajectories the spacing matrix is taken from the first periodic frame; the density is therefore referenced to that cell geometry."]]]]